{
  "tool_id": "travel.flight.search",
  "display_name": "Flight Search",
  "summary": "Find candidate flights through broker-approved flight sources.",
  "kind": "travel",
  "status": "active",
  "version": "2026-03-22",
  "tags": ["travel", "flight", "search", "high-level"],
  "capability_template": {
    "action_type": "read",
    "resource_type": "travel",
    "risk_level": "low",
    "approval_policy": "auto",
    "ttl_seconds": 900,
    "audit_level": "summary",
    "quota": { "max_calls": 20, "per_window_seconds": 3600 }
  },
  "capability_bindings": [
    {
      "capability_id": "travel.flight.search",
      "route": "travel_flight_search",
      "purpose": "Broker-mediated flight lookup."
    }
  ],
  "input_schema": {
    "type": "object",
    "properties": {
      "query": { "type": "string" },
      "locale": { "type": "string" },
      "limit": { "type": "integer", "minimum": 1, "maximum": 5 }
    },
    "required": ["query"]
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "mode": { "type": "string" },
      "query": { "type": "string" },
      "retrieved_at": { "type": "string" },
      "results": { "type": "array" },
      "sources_used": { "type": "array", "items": { "type": "string" } }
    }
  },
  "source_policy": {
    "allowed_sources": ["duckduckgo", "public_travel_web"]
  },
  "execution_rules": {
    "mediation_required": true,
    "requires_source_policy": true,
    "default_limit": 5
  },
  "response_contract": {
    "must_identify_sources": true,
    "must_identify_retrieval_time": true,
    "result_wording": "Present flight schedule options as candidates, not final booking advice."
  }
}
