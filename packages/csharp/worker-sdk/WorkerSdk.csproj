<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>WorkerSdk</RootNamespace>

    <PackageId>WorkerSdk</PackageId>
    <Version>1.0.0</Version>
    <Description>功能池 Worker 開發套件 — Worker 進程框架（連線 + 註冊 + 心跳 + 分派接收）</Description>
    <Authors>Bricks4Agent</Authors>
  </PropertyGroup>

  <ItemGroup>
    <!-- 複用 FrameCodec + OpCodes（Binary TCP 協議） -->
    <ProjectReference Include="..\cache-protocol\CacheProtocol.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" Version="8.0.0" />
  </ItemGroup>

</Project>
