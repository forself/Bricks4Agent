# 待辦 — 2026-05-27 session 結束盤點(換機器 handoff)

承接 [TODO-2026-05-26.md](TODO-2026-05-26.md)。今早把昨天 P0/P2 都吃掉、Scanner Hybrid Phase 1 主幹 ship 到 VPS shadow 跑起來。

---

## ✅ 今天完成(2026-05-27 上午到中午)

### P0 — 全部答完
- ✅ Scanner universe 標準 = **兩者聯集**(跨時框 ≥4/5 ∪ 1d ≥15%)
- ✅ APT = **不進、列觀察單**(scan10 主場 4/5 / 4% 收回去)
- ✅ 第二支 scanner = **ts_momentum**(t=3.93、跟 widepz 主場互補)
- ✅ Scanner budget 草案維持(單 scanner ≤ 50%、總和 ≤ 80%、shadow 期不重要)
- ✅ Phase 2 子帳戶決策維持(先 hack、Phase 3 才引入)

### P2 — 3 輪 deep dive + universe 分析全做完
- ✅ `harm_prz_scan10` deep dive — t=**7.69** / Sharpe 0.99 / DD 11(risk-adjusted 王者)
- ✅ `ts_momentum` deep dive — t=3.93、universe 跟 scan10 主場零重疊
- ✅ `tsmom_widepz` / `tsmom_harm_prz_scan10` ensemble 補測 — t=4.29/3.95 顯著、**但 LS Sharpe -0.44/-0.47 ❌**(ensemble 不適合當 scanner)
- ✅ Universe 交集分析 — 4 個 scanner 配置 + 衝突點盤點寫進 [docs/reports/ScannerUniverse-2026-05-27.md](reports/ScannerUniverse-2026-05-27.md)

### P1 — Phase 1 主幹完工
- ✅ **Step A** Schema:`ScannerLegEntry` + `ScannerActiveLegEntry` model + `EnsureTable` 註冊(commit `6c57049`)
- ✅ **Step B.1** `ProcessScannerAsync` 骨架(load + iterate + candidate filter、commit `2ee42f1`)
- ✅ **Step B.2** Signal fetch + ranking(quote.ohlcv + strategy.signal worker、commit `17152a9`)
- ✅ **Step B.3** Shadow dispatch + 冪等鎖(`scanner_id + symbol + signal_bar_ts`、commit `763ed68`)
- ✅ **P3 seed** SQL — 4 scanner shadow=1 啟動腳本(commit `31deea8`)

### VPS 部署 — Phase 1 shadow 跑起來
- ✅ VPS Contabo 新加坡(`b4a-trading`)拉了 14 個 commit、rebuild b4a-broker image(141s)
- ✅ `EnsureTable` 自動建好 `scanner_legs` + `scanner_active_legs` 兩張新表
- ✅ `scripts/scanner-seed.sql` 跑完、4 scanner 入表:
  ```
  scan10_scanner   | harm_prz_scan10        | universe=7 | max=3 | shadow=1
  widepz_scanner   | harm_prz_scan10_widepz | universe=7 | max=3 | shadow=1
  tsmom_1d_scanner | ts_momentum            | universe=3 | max=2 | shadow=1
  tsmom_1w_scanner | ts_momentum            | universe=4 | max=3 | shadow=1
  ```
- ✅ Broker `Up healthy`、AutoTrader sweep 跑起來、第一個 cycle 4 個 scanner 都掃了

---

## ⚠ 今天發現的新問題(換機器後優先解決)

### 🔴 P0' — Scanner 跟 watchlist 衝突、candidates 全 0

VPS 上的 `auto_trade_watchlist` 有 **50+ 個 active=1 watch**(BingX 6 個真錢 + Alpaca 13 個美股 + Binance 30+ 加密),把 scanner universe 的幣**全部都占用**:

```
Scanner scan10_scanner    universe=7 occupied=51 candidates=0 slots=3 shadow=True
Scanner widepz_scanner    universe=7 occupied=51 candidates=0 slots=3 shadow=True
Scanner tsmom_1d_scanner  universe=3 occupied=51 candidates=0 slots=2 shadow=True
Scanner tsmom_1w_scanner  universe=4 occupied=51 candidates=0 slots=3 shadow=True
```

每個 scanner universe symbol 都有現存 watch(LTC×accel_momentum / OP×ichimoku / NEAR×adx_di / ...),按目前避讓邏輯 scanner 永遠不開。

**三條路**(下個 session 決定):

- **A. 清 watchlist**(操作面):那 50 個多半是早期實驗,保留 6 個真錢腿(BTC/BNB/SUI/SOL/UNI/ETH),其他 `active=0` 暫停,讓 scanner 接管 alt-coin。最乾淨。
- **B. Code 改 conflict**:把衝突規則從 `symbol` 改成 `(strategy, symbol)` tuple — 同幣不同策略可共存。風險:同幣兩方向訊號互打。
- **C. Code 改 conflict 2**:只避開「真錢腿」(active=1 AND shadow=0 AND budget_pct>0),實驗 watch 視為「可被 scanner 蓋過」。適合現況。

我建議從 **A** 開始(快、保守、不動 code),如果保留實驗 watch 有理由再考慮 **C**。

### Phase 1 餘下工程(shadow 跑期間慢慢補)

- [ ] **B.4** scanner close-side lifecycle — 反向訊號 close shadow leg + 累積 shadow PnL(沒這個 shadow 永遠開不關、4 週評估算不出 PnL)
- [ ] **B.3b** Real dispatch path — 4 週 shadow 達標才動、含 risk gate + 真實派單路徑
- [ ] **C** `trading-manage.html` 加 Scanner Legs 分頁(view + edit universe/budget/enable)
- [ ] **D** Approval flow 整合 four-tier `IApprovalService`(現在 shadow 不需要、real 上線必補)
- [ ] **E** 整合測試 — scanner 啟用/停用/universe 變更/撞核心腿避讓

---

## 🟢 P3 — Shadow 4 週倒數開始

⏰ **Shadow 啟動日期:2026-05-27**(broker rebuild 跑 scanner 起算)
⏰ **預計回顧日期:2026-06-24**(4 週後)

但是因為 P0' candidates=0、目前 shadow 實際**沒在累積 PnL**。要嘛先解 P0'、要嘛接受 shadow 倒數延後到「scanner 真的開到第一個 shadow leg」之後再計算 4 週。

評估標準(維持昨天訂的):
- shadow PnL ≥ +5% 累積
- DD < 20%
- 至少 10 個 shadow legs 跑完整個 lifecycle(close 過、有 PnL)
- 真實 funding rate 加入計算(P5 還沒做、別跳過)

---

## 🔵 P4 — Cleanup(VPS 上的 untracked,需手動決策)

VPS `/opt/b4a` git status untracked:
- `allocate-reports/` `data/` `equity-log.csv` — 自動生成、`.gitignore` 進
- `scripts/backup-daily.sh` `scripts/equity-snapshot.sh` — 部署腳本、應 commit 或加 gitignore
- `scripts/strat-validate-bin/` — build artifact、加 gitignore
- `discord-bots/bot-node/access.json.bak` — backup 檔、加 gitignore

另:這台 Mac 那 4 個檔案根本不存在(`tools/decorr-analysis/`、`tools/deploy-decorr4.ps1`、`docs/backend-api-guide.md`、`SHARE-PLAN.md`)— 應該在另一台 Mac 上、需要那邊 commit 或 .gitignore。

---

## 🟣 P5 — 系統面(維持昨天清單)

- [ ] `strat-validate` 加 vol-targeting / risk-parity 配重模擬
- [ ] SUI/SOL/UNI 在 `param-stability` 補測(本報告未涵蓋的 3 真錢腿)
- [ ] **真實 funding rate 入回測**(P3 評估前必補、目前 0.010%/8h 假設、抓 BingX 歷史校準)
- [ ] BingX 子帳戶開通流程(Phase 3 前置)

---

## ⚠ 紀律 / 永久守則(維持)

- ✋ 任何 ETH 換腿 / 新策略上線必須先過 pool t-stat(t ≥ 3、95% CI 下界 > 0)
- ✋ 單一漂亮 walk-forward 結果(Sharpe ≥1.5 / WR ≥70%)一律標 ⚠ 待 pool 驗
- ✋ Shadow 4 週紀律不可繞、即使 t-stat 顯著
- ✋ 初始配重一律砍半

---

## 真錢現況(維持不動)

| 腿 | 策略 | budget% | t-stat | 動作 |
|---|---|---:|---:|---|
| BTC | decorr4_ls | 120 | 3.11 ✅ | 不動 |
| BNB | ma_regime_trend | 60 | 2.52 ⚠ | 不動 |
| SUI | dual_mom_ls | 35 | 3.21 ✅ | 不動 |
| SOL | dual_thrust | 30 | 2.67 ✅ | 不動 |
| UNI | ts_momentum | 30 | 4.10 ✅ | 不動 |
| ETH | mfi | 25 | ❌ | 等 shadow 4 週(實際倒數待 P0' 解 + 第一個 scanner leg 開) |

ETH 換腿候選:`harm_prz_scan10_widepz` / `harm_prz_scan10` / `ts_momentum`(都 pool t-stat 顯著)。

---

## 今日 commit 對應

| commit | 內容 |
|---|---|
| `6c57049` | feat(scanner): Phase 1 schema — ScannerLegEntry + ScannerActiveLegEntry models |
| `82d3cc1` | research: Scanner Universe 交集分析 — 3 支 scanner 主場分配 + ensemble ❌ |
| `2ee42f1` | feat(scanner): Phase 1 Step B.1 — ProcessScannerAsync 骨架 |
| `17152a9` | feat(scanner): Phase 1 Step B.2 — signal fetch + candidate ranking |
| `763ed68` | feat(scanner): Phase 1 Step B.3 — shadow dispatch + persist + idempotency 鎖 |
| `31deea8` | feat(scanner): Phase 1 P3 — seed SQL 啟動 4 個 shadow scanner |

VPS 部署狀態(僅紀錄,不在 commit 中):
- HEAD: `31deea8` synced
- broker rebuild: 141s、Up healthy
- scanner_legs / scanner_active_legs 表已建、4 列 scanner seed 完成
- 第一個 sweep cycle 跑過、所有 scanner candidates=0(衝突點 P0')

---

## 下個 session 起手第一件事

**解 P0' scanner candidates=0** — 建議跑 **A**(清 watchlist):

```bash
ssh b4a
# 看哪些 watch 是早期實驗(active=1 但沒 budget_pct、或策略已 t-stat 降級)
docker exec b4a-broker sqlite3 /data/broker.db \
  "SELECT exchange, symbol, strategy, budget_pct, shadow, active FROM auto_trade_watchlist ORDER BY exchange, symbol;"

# 確認哪些要保留(6 真錢腿) vs 暫停(其他)
# 然後逐筆 UPDATE active=0(暫停 vs DELETE 看你喜好)
```

或先看一輪、再決定走 A/B/C。
