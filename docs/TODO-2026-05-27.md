# 待辦 — 2026-05-27 session 結束盤點(handoff 更新)

承接早上的 [初始 handoff 版本(已被本檔覆寫)](TODO-2026-05-26.md) → 下半天解 P0' + 上 B.4 後重寫。

---

## ✅ 今天完成(完整)

### 早上(已 handoff)
- P0 全部答完(scanner universe 標準、APT 觀察、第二 scanner、budget 上限、子帳戶時機)
- P2 3 輪 deep dive(scan10/widepz/ts_momentum + ensemble ❌)
- P1 Step A-B.3:schema + skeleton + signal fetch + shadow dispatch + 冪等鎖
- P3 seed:4 scanner shadow=1 入 DB
- VPS 部署:b4a-broker rebuild、scanner_legs 表自動建、第一 sweep cycle 4 scanner 都掃

### 下午(本次 session)
- 🔴 **P0' 解了**(commit `3db6642`):
  - 換 Option C(scanner conflict 規則改 budget>0)、繞開 watchlist re-activation mystery
  - 順手修 open_time GetInt64 對 String 拋 InvalidOperationException 的 bug
  - 驗證:scanner candidates 4-7、3 個 shadow leg 順利開出(TRX×2、XRP×1)
- 🟢 **B.4 close-side lifecycle 完工**(commit `1656b19`):
  - Schema:`ScannerActiveLegEntry` 加 `ClosedAt` / `ExitPrice` / `RealizedPnlPct` / `CloseReason`
  - Entry price capture:shadow 模式用 lastBar.close 填 leg.EntryPrice + PeakMark
  - Close logic:`CloseScannerActiveLegsAsync` 每 cycle 在開新腿前跑、反向訊號用 bar close 算 PnL(含 0.16% round-trip 成本)
  - Active query filter:`WHERE closed_at IS NULL`、closed legs 不算容量
  - Migration:`AutoTraderDbMigrations` 補 4 個 ALTER 欄、既有 DB 自動相容
- VPS 重 deploy、orphan B.3 legs(EntryPrice=0)清掉、fresh B.4 跑、新 leg entry price 都正確記錄

---

## ⚠ 未解 mystery

### Watchlist re-activation source 不明

- 現象:SQL UPDATE auto_trade_watchlist SET active=0 後 60 秒內、32 個 watch 被某個 in-memory write-back 重置回 active=1
- 已排除:PortfolioReconciler(只 add shadow=true 新單)、bot-node(只有 docs ref)、明顯 timer
- 對 B.4 影響:**無**(Option C 已繞開、scanner 不靠 watchlist 狀態)
- 對核心腿影響:**無**(45 個被反覆 active 的腿都 budget=0、不會下單)
- 待:有空再追、但不擋路。線索:updated_at 集中在 sweep 邊界後幾秒、用 sqlite trigger 或加 LogTrace 到 PersistWatch 可定位
- 詳見 [[project_scanner_p0_option_c]]

---

## 🟡 Phase 1 剩下(shadow 跑期間慢慢補)

- [ ] **B.3b** Real dispatch path — 4 週 shadow 達標才動、含 risk gate + 真實派單路徑 + 跨 exchange symbol normalization(scanner default `binance` vs 核心腿 `bingx` 同名不雙開)
- [ ] **C** `trading-manage.html` 加 Scanner Legs 分頁(view + edit universe/budget/enable)
- [ ] **D** Approval flow 整合 four-tier `IApprovalService`(real 上線必補、shadow 不需要)
- [ ] **E** 整合測試 — scanner 啟用/停用/universe 變更/撞核心腿避讓
- [ ] **F** TP/SL 觸發 close(等策略 emit StopPrice/TargetPrice 進 leg、Phase 2)
- [ ] **G** Funding rate 入 PnL(P5、真實 BingX 歷史 funding)

---

## 🟢 P3 — Shadow 4 週倒數

⏰ **Shadow 累積 PnL 倒數開始日:2026-05-27**(P0' 解 + B.4 完工後第一個 sweep 出 shadow leg)
⏰ **預計回顧日期:2026-06-24**(4 週後)

當前 3 個 shadow leg(2026-05-27 06:58 UTC sweep):
- tsmom_1d_scanner | TRXUSDT | long  | entry=0.3635
- tsmom_1w_scanner | TRXUSDT | long  | entry=0.3679
- tsmom_1w_scanner | XRPUSDT | short | entry=1.348

scan10_scanner / widepz_scanner 還沒 picks(candidates 都 hold / 低 conf、等 PRZ 形態)。

評估標準(維持):
- shadow PnL ≥ +5% 累積
- DD < 20%
- 至少 10 個 shadow legs 跑完整個 lifecycle(有 close 過、有 realized PnL)
- 真實 funding rate 加入計算

---

## 🔵 P4 — Cleanup

VPS:
- `allocate-reports/` `data/` `equity-log.csv` — 自動生成、加 .gitignore
- `scripts/backup-daily.sh` `scripts/equity-snapshot.sh` — 部署腳本、commit 或 gitignore
- `scripts/strat-validate-bin/` — build artifact、gitignore
- `discord-bots/bot-node/access.json.bak` — backup、gitignore

本機:`tools/decorr-analysis/` `tools/deploy-decorr4.ps1` `docs/backend-api-guide.md` `SHARE-PLAN.md`(SHARE-PLAN 明確「不 push」)

---

## 🟣 P5 — 系統面(長期)

- [ ] strat-validate 加 vol-targeting / risk-parity 配重模擬
- [ ] SUI/SOL/UNI 在 param-stability 補測(本報告未涵蓋的 3 真錢腿)
- [ ] **真實 funding rate 入回測**(P3 評估前必補、目前 0.010%/8h 假設)
- [ ] BingX 子帳戶開通流程(Phase 3 前置)
- [ ] (optional)追 watchlist re-activation mystery 根因

---

## ⚠ 紀律 / 永久守則(維持)

- ✋ 任何 ETH 換腿 / 新策略上線必須先過 pool t-stat(t ≥ 3、95% CI 下界 > 0)
- ✋ 單一漂亮 walk-forward 結果(Sharpe ≥1.5 / WR ≥70%)一律標 ⚠ 待 pool 驗
- ✋ Shadow 4 週紀律不可繞、即使 t-stat 顯著
- ✋ 初始配重一律砍半

---

## 真錢現況(維持不動)

| 腿 | 策略 | budget% | t-stat | 動作 |
|---|---|---:|---:|---|
| BTC | decorr4_ls | 120 | 3.11 ✅ | 不動 |
| BNB | ma_regime_trend | 60 | 2.52 ⚠ | 不動 |
| SUI | dual_mom_ls | 35 | 3.21 ✅ | 不動 |
| SOL | dual_thrust | 30 | 2.67 ✅ | 不動 |
| UNI | ts_momentum | 30 | 4.10 ✅ | 不動 |
| ETH | mfi | 25 | ❌ | 等 shadow 4 週、再用顯著候選換 |

---

## 今日 commit 對應(下午段)

| commit | 內容 |
|---|---|
| `3db6642` | fix(scanner): P0' 解 — Option C conflict + open_time 型別容錯 |
| `1656b19` | feat(scanner): Phase 1 Step B.4 — close-side lifecycle + entry price capture |

VPS 部署狀態:
- HEAD: `1656b19` synced
- broker rebuild × 2、Up healthy
- scanner_active_legs 4 個新欄已 migrate
- 第一個 B.4 sweep:3 shadow legs 開、entry price 正確記錄

---

## 下個 session 起手第一件事

**等 close-side 第一次跳動的證據**:

```bash
ssh b4a
docker logs --since 12h b4a-broker 2>&1 | grep '\[SHADOW\].*closed' | tail -20
```

如果有 closed leg 出現、看 realized_pnl_pct 數字是否合理(±5% 範圍內合理、超過 ±20% 要 debug)。

接著可挑:
- 補 B.3b real dispatch path(等 shadow 達標、不急)
- 補 C trading-manage.html Scanner Legs UI(隨時可做)
- 補 P5 funding rate 入 PnL(shadow 評估前必補)
- 追 watchlist re-activation mystery(optional)
